ktor {
  deployment {
    port = 9999
    port = ${?PORT}
  }
  application {
    modules = [ ru.netology.ApplicationKt.module ]
  }
}

crud {
  upload {
    dir = ./uploads
    dir = ${?UPLOAD_DIR}
  }
}